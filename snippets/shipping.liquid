<style>
    .delivery-box {
  background: #f8fafc;
  padding: 14px 16px;
  border-radius: 10px;
  font-size: 15px;
  line-height: 1.6;
  max-width: 420px;
  margin-top: 10px;
}

.delivery-row {
  display: flex;
  gap: 6px;
  margin-bottom: 6px;
  align-items: center;
}

.delivery-row:last-child {
  margin-bottom: 0;
}
</style>

{% assign today = 'now' | date: "%s" %}
{% assign seconds_in_day = 86400 %}

{%- comment -%} Delivery date calculations {%- endcomment -%}
{% assign dncr_start = today | plus: seconds_in_day*2 | date: "%b %d" %}
{% assign dncr_end = today | plus: seconds_in_day*3 | date: "%b %d" %}

{% assign pan_start = today | plus: seconds_in_day*5 | date: "%b %d" %}
{% assign pan_end = today | plus: seconds_in_day*7 | date: "%b %d" %}

{% assign intl_start = today | plus: seconds_in_day*15 | date: "%b %d" %}
{% assign intl_end = today | plus: seconds_in_day*25 | date: "%b %d" %}

<div class="delivery-box">
  <div class="delivery-row">
    🚚🇮🇳 <span>Delivery Delhi NCR:</span> <strong>{{ dncr_start }} — {{ dncr_end }}</strong>
  </div>
  <div class="delivery-row">
    🚛📍 <span>Delivery PAN India:</span> <strong>{{ pan_start }} — {{ pan_end }}</strong>
  </div>
  <div class="delivery-row">
    ✈️🌎 <span>International Delivery:</span> <strong>{{ intl_start }} — {{ intl_end }}</strong>
  </div>
</div>
